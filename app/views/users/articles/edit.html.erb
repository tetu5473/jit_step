<h1 class="text-center">記事の編集</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= form_with model: @article, url: users_article_path(@article), local: true, method: :patch do |form| %>
        <div class="form-group mb-3">
          <%= form.label :title, "タイトル" %>
          <%= form.text_field :title, class: "form-control w-100" %>
        </div>

        <div class="form-group mb-3">
          <%= form.label :content, "内容" %>
          <%= form.text_area :content, class: "form-control w-100" %>
        </div>

        <div class="form-group mb-3">
          <%= form.label :image, "画像" %>
          <%= form.file_field :image, class: "w-100" %>
        </div>

        <div class="text-center mb-3">
          <%= form.submit "更新する", class: "btn btn-primary" %>
        </div>
      <% end %>

      <div class="text mt-3">
        <%= link_to '戻る', users_articles_path %>
      </div>
    </div>
  </div>
</div>
